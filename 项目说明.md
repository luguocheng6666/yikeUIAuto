# yikeUIAuto 项目说明（重读版）

> 面向"隔了几年要重新捡起来"的场景整理。所有结论都来自对当前代码的实扫，不是推测。

---

## 一、一句话定位

**关键字驱动 + 数据驱动的 UI 自动化框架**。测试用例不写在 Python 里，而是写在 Excel 里 —— 你用「关键字 + 定位 + 数据」三件套描述测试步骤，框架读表、动态生成 unittest 用例、驱动浏览器或 Windows 桌面程序执行，最后把 Passed/Failed 回写到同一张 Excel，并产出带截图的 HTML 报告。

技术栈：Python 3 + Selenium 3.141（Web）+ pywinauto（Windows 桌面端）+ openpyxl/xlrd（Excel 读写）+ unittest + HTMLTestRunner。

---

## 二、它测什么（3 套被测系统）

| 类型 | 系统 | 地址/路径 |
|---|---|---|
| Web | 翼课网 **CRM 营销管理系统** | `http://crm.ekwing.com/index/login` |
| Web | **智慧考试管理系统** | `http://wisdom.100efly.com/page/pc/admin/login?redirect=%2Fexam-manage%2Funified-manage%2Findex` |
| 桌面 | **普通话分级考试系统（监考机）** | `C:\普通话分级考试系统-监考机\Teacher.exe` |

（旧版 element.xml 里还留着 `www.ekwing.com` 官网登录/注册的定位器，是更早的痕迹。）

### 用例资产规模

- **22 个用例，514 行步骤**，都在 `Data/testdata.xlsx`
- 覆盖：登录、账户（订单/充值/明细）→ 教师管理（增删改查/重置密码/改状态）→ 学生管理（同）→ 6 类角色权限验证 → 桌面端正考登录
- 最复杂的用例：`student_update` 44 步、`student_insert` 42 步
- 目前只有 `crm_login` 的`是否需要执行` = `y`，其余 21 个都是 `N`（默认全关）

---

## 三、项目生命周期（帮你回忆时间线）

| 时间 | 事件 |
|---|---|
| 2023-04 ~ 2023-09 | **密集使用期**。`BackUP/` 归档了 4/5/7/8/9 月共 220+ 次执行记录（报表 + Excel 快照）；`Logs/` 留存到 2023-09-13 |
| 2023-09-13 | 最后一批代码修改（各文件 header 里的 LastEditTime） |
| 2023-09 之后 | 停用。期间 Chrome 自动升级到 152，驱动仍停在 116 |
| 2026-09-08 | 重新捡起，修复环境问题并跑通（见 CODEBUDDY.md） |

---

## 四、核心设计思想：三层分离

这是整个项目最值得记住的一点：

```
TestSteps  →  做什么（步骤/关键字/定位）      ← 流程
TestDatas  →  输入什么、期望什么               ← 数据 + 断言
framework  →  怎么做（Selenium/pywinauto 实现）← 引擎
```

带来的能力：
- **同一套步骤换数据 = 多组用例**：`input` 关键字的**输入值**不写在步骤里，而是从 TestDatas 的 `Input1/Input2/...` 按顺序取。
- **改断言不用改步骤**：断言的定位（`Type`/`Expression`）和期望值（`ExpectedResult`）也在 TestDatas。
- **一条用例跑多次**：TestDatas 的 `Cycle` 列。

---

## 五、目录地图

| 路径 | 职责 |
|---|---|
| `testsuites/TestRunner.py` | **总入口**。生成用例 → discover → 跑 → 出 HTML 报告 → 备份 |
| `framework/keywordsFrameword.py` | **引擎核心**：读 Excel、动态生成 `test_*` 方法、关键字分发（注意文件名拼错成 `Frameword`） |
| `framework/basepage.py` | `BasicPage`：**所有页面动作的实现**（找元素/点击/输入/下拉/截图/断言/窗口切换），Selenium 与 pywinauto 双分支 |
| `framework/browser_engine.py` | `BrowserEngine`：起浏览器（Chrome/Firefox/IE）或起 Windows 程序 |
| `framework/excelutil.py` | `excel_readWrite`：Excel 读写（openpyxl 写、xlrd 读） |
| `framework/logger.py` | 日志（按天切分，`Logs/logs.log`，保留 5 份） |
| `framework/send_email.py` | 失败时发邮件告警（QQ SMTP，**凭据硬编码**，当前在 TestRunner 里被注释） |
| `framework/upload.py` | `backup()` 本地归档；`upload()` SFTP 上传（当前被注释） |
| `framework/exceltest.py` / `myunittest.py` | 早期 demo / 基类，主流程没用到 |
| `keywordsDriver/keywordKey.py` | 关键字名常量（全小写） |
| `keywordsDriver/excelKey.py` | Excel 的 sheet 名 + 列名常量 |
| `keywordsDriver/ColorKey.py` | 结果单元格配色 |
| `keywordsDriver/keywordAction.py` | `Action`，继承 `BasicPage`，引擎实际调用的对象 |
| `Data/testdata.xlsx` | **用例资产**（3 张主表 + 2 张说明表） |
| `Data/element.xml` / `element.py` | 早期 XML 元素库，已被 Excel 内联定位取代 |
| `tools/` | 浏览器驱动 + 3 个 HTMLTestRunner 变体 + 2 个 ddt 版本 |
| `pageobjects/` | **空目录** —— 当年的 POM 设想没落地 |
| `reports/` | 报告产物 `result.html` |
| `BackUP/` | 按 `年/月` 归档的历史报告 + Excel 快照 |
| `test.py` / `test copy.py` | 桌面端自动化的试验脚本（大量注释掉的代码） |

---

## 六、Excel 三张主表（字段级说明）

### 1. `测试用例`（用例总开关）

| 列 | 说明 |
|---|---|
| `TCID` | 用例唯一标识，如 `crm_login`，三张表靠它关联 |
| `用例名称` / `用例描述` | 说明文字，为空会报错 |
| `是否需要执行` | **总开关**，`y` 才会被收集（当前只有 crm_login 是 y） |
| `执行时间` / `结果` | 预留回写位 |
| 第 7 列（无表头） | 备注，你当时统一写了「调试通过」 |

### 2. `TestDatas`（数据 + 断言，22 行）

| 列 | 说明 |
|---|---|
| `TCID` / `CaseId` / `Data_name` / `Summary` | 关联标识与描述 |
| `Runmode` | `Y` 才执行（与上一表的`是否需要执行`是**与**关系） |
| `Input1` ~ `Input8` | **按顺序**喂给步骤里的 `input` / `send_keys` 关键字 |
| `selector1` / `selector2` | 按顺序喂给 `selectbyindex/text/value` |
| `Type` / `Expression` / `ExpectedResult` | **断言三件套**，多个断言用英文逗号分隔，按出现顺序一一配对 |
| `verifiedcodeXY` | 滑块验证码的相对坐标，供 `CodeSlide` 用 |
| `Cycle` | 该条数据执行几次（0 会被自动改成 1） |
| `ErrMsg` / `Result` / `StartTime` / `RunTime` | **框架回写**的结果区 |

### 3. `TestSteps`（514 行步骤）

`TCID` | `步骤序号` | `测试步骤描述` | `关键字` | `操作元素定位方式` | `操作元素定位表达式` | `操作值` | `是否需要执行`

- 定位方式 + 定位表达式会被拼成 `方式=>表达式`，例如 `name=>user_name`
- 支持的定位前缀：`i`/`id`、`n`/`name`、`cn`/`class_name`、`lt`/`link_text`、`plt`/`partial_link_text`、`tn`/`tag_name`、`x`/`xpath`、`cs`/`css_selector`
- 用 `=>` 而不是 `=` 分隔，是因为 xpath 里经常含 `=`

### 一个真实例子（`crm_login`，11 步）

| 步骤 | 关键字 | 定位 | 操作值 |
|---|---|---|---|
| 001 | `open_browser` | | `Chrome` |
| 002 | `maxwindow` | | |
| 003 | `open_url` | | `http://crm.ekwing.com/index/login` |
| 004 | `input` | `name=>user_name` | （取 TestDatas 的 Input1） |
| 005 | `input` | `name=>password` | （取 Input2） |
| 008 | `sleep` | | `2` |
| 009 | `screenshots` | | |
| 010 | `assertEqual` | （取 Type/Expression） | （取 ExpectedResult） |
| 011 | `quite_browser` | | |

---

## 七、关键字全表（35 个）

`keyword_template` 表列了 35 个；代码里实际还多实现了 3 个（`get_win`、`mouse_click`、`Close_win`，是后来做桌面端时加的，模板没同步）。

**浏览器 / 应用**：`open_browser`、`maxwindow`、`open_url`、`start_app`、`get_win`、`close_browser`、`Close_browser`、`Quite_browser`、`Close_win`、`Back`、`Refresh`

**元素操作**：`click`、`input`、`send_keys`、`clear`、`selectbyindex`、`selectbytext`、`selectbyvalue`、`Move_mouse_to_element`、`CodeSlide`（滑块）、`mouse_click`（桌面坐标点击）、`Exeucejs`（删属性，绕开日期控件只读）

**窗口 / 框架**：`swith_window_handle_by_title`（标题子串即可）、`swith_window_handle_by_index`、`swith_frame`

**控制**：`sleep`、`screenshots`

**断言（13 个）**：`assertEqual`、`assertNotEqual`、`AssertIn`、`AssertNotIn`、`assertTrue`、`assert_Is_Display`、`assert_Is_Enabled`、`assert_Is_Exist`、`assert_Is_NotExist`、`assert_Is_Number`、`assert_Number_between`

---

## 八、执行全流程（从敲命令到出报告）

```
python testsuites/TestRunner.py
  │
  ├─1─ __generateTestCases()
  │     读「测试用例」表取 是否需要执行=y 的 TCID
  │     再读 TestDatas 取 Runmode=Y 且 TCID 命中的行
  │     按 Cycle 展开，动态 setattr 出 test_<CaseId>_<TCID>_<Data_name> 方法
  │
  ├─2─ unittest discover("framework","keywordsFrameword.py", top_level_dir=rootPath)
  │     ⚠️ top_level_dir 不能省，否则收集到 0 个用例（静默失败）
  │
  ├─3─ 每个用例 → executeCase()
  │     ├─ getCaseStep() 取该 TCID 的步骤
  │     ├─ 逐条 if/elif 分发关键字 → Action（继承 BasicPage）
  │     ├─ 断言：从 TestDatas 按 assertnum 顺序取 Type/Expression/ExpectedResult
  │     ├─ 截图：base64 存进 self.imgs
  │     └─ finally：回写 RunTime，关浏览器
  │
  ├─4─ HTMLTestRunner 生成 reports/result.html（含 echarts 饼图 + 内嵌截图）
  │
  └─5─ backup() 把报告 + testdata.xlsx 复制到 BackUP/<年>/<月>/
```

### 断言机制（最容易忘的细节）

- 断言的**定位方式和期望值都在 TestDatas**，不在步骤里
- 一条用例多个断言 → `Type`/`Expression`/`ExpectedResult` 三列都用逗号分隔，按**出现顺序**配对（引擎里 `assertnum` 递增）
- `assertTrue` 时，`ExpectedResult` 写不写都行（写了也不判断）
- `assert_Number_between` 的期望值写成区间，如 `1-100`
- `assert_Is_Exist` / `assert_Is_NotExist`：期望值写 `null`/`false`/`0` 视为 False，其它视为 True
- **整条用例一个断言都没配，且没抛异常 → 直接记 `Passed`**

### 结果回写机制

`Result` / `ErrMsg` / `StartTime` / `RunTime` 这四个单元格，**在读取时会被替换成 `"行号,列号"` 字符串**（`excelutil.get_xls` 里的 `NeedXY` 逻辑），引擎靠这个坐标回写。所以你打开 Excel 看到这些列里是 `2,22` 这样的数字，是正常现象，不是脏数据。

配色：`Passed` 蓝 `FF2C24D1`，`Failed` 红 `FFFC0308`，`Error` 暗红 `FFb64645`。

---

## 九、双引擎：Web 与桌面端怎么切换

判据很原始 —— 看 driver 的字符串里含不含关键字：

```python
if 'selenium' not in str(self.driver):   # 走 pywinauto 分支
```

**桌面端流程**（`zk_login` 用例）：
1. `start_app` → `C:\普通话分级考试系统-监考机\Teacher.exe`
2. `get_win` → 窗口标题，如 `CLoginWnd`
3. `mouse_click` → 操作值填 `x,y`，是**相对窗口左上角的偏移**（不是屏幕绝对坐标）
4. `send_keys` → 直接打字
5. 截图走 `window.capture_as_image()` → base64

桌面端用例在 `finally` 和 `tearDownClass` 里会跳过关浏览器逻辑（靠 `pywinauto` in str(driver) 判断）。

---

## 十、新增一条用例的完整操作

1. **`测试用例` 表**加一行：填 `TCID`、`用例名称`、`用例描述`、`是否需要执行=y`
2. **`TestSteps` 表**加步骤，每行 `TCID` 与上面一致，填步骤序号、描述、关键字、定位方式/表达式、操作值、`是否需要执行=y`
3. **`TestDatas` 表**加一行：`TCID`、`CaseId`、`Runmode=Y`、`Data_name`，输入值填进 `Input1..`，断言填进 `Type`/`Expression`/`ExpectedResult`
4. 确认 `Data/testdata.xlsx` **没有在 Excel/WPS 里打开**（否则回写报 PermissionError）
5. `python testsuites/TestRunner.py`

---

## 十一、现状问题清单（为优化做准备）

### 结构性
1. **关键字分发是 ~500 行 if/elif**（`executeCase`），加一个关键字要改三处 → 可改成字典/装饰器注册表
2. **大量重复定义**：`click`、`back`、`maxwindow` 在 `basepage.py` 里各定义了两遍（后者覆盖前者）
3. **`pageobjects/` 是空目录** —— POM 没落地，定位表达式散落在 Excel 里，页面一改要改一堆行
4. **`element.xml` / `element.py` 已废弃但还在**，用了 Python 3.9 移除的 `getchildren()`
5. **`tools/` 里躺着 3 个 HTMLTestRunner 变体 + 2 个 ddt 版本**，实际只用 echarts2 那个

### 工程化
6. **凭据硬编码**：`send_email.py` 里 QQ 邮箱授权码明文；`upload.py` 依赖 setting 里的服务器账号
7. **无失败重试**（TestRunner 里 `retry=0`，框架其实支持）
8. **无并发**：串行执行，22 个用例全开会很慢
9. **无 CI 集成**：没有 Jenkins/GitHub Actions 配置
10. **Excel 当数据库**：无法并发写、无法 code review、难 diff

### 环境/版本
11. **Selenium 3.141 已停维护**，用了 `executable_path=`（Selenium 4 已移除）
12. **报告的 echarts 走 `cdn.bootcss.com`**，该 CDN 已停止服务，图表大概率加载不出来
13. **浏览器驱动靠手工下载**，Chrome 一升级就断（这次就是这么坏的）
14. **无日志/归档清理策略**：`BackUP/` 已积累 440+ 文件

### 用例数据本身
15. **步骤序号有重复**（crm_login 里 008/009 出现两次）
16. **21/22 个用例处于关闭状态**，且页面结构 3 年没维护，重新打开大概率要修定位
17. **`keyword_template` 缺 3 个后加的关键字**，与实际实现不同步

---

## 十二、建议的优化优先级（供你决策）

**第一梯队（低风险、见效快）**
- Selenium 3 → 4 迁移（顺带去掉手工驱动管理，用 Selenium Manager 自动匹配）
- 关键字分发改注册表，消灭 500 行 if/elif
- 凭据移出代码，走配置文件/环境变量
- 修掉重复定义、废弃文件、失效 CDN

**第二梯队（结构性改造）**
- 用例从 Excel 迁到 YAML/JSON（可 diff、可 review、可并发），或保留 Excel 但加一层导入导出
- 落地 POM，把定位表达式从 Excel 收拢到页面类
- 加失败重试 + 并发执行

**第三梯队（工程化）**
- CI 集成、失败告警、趋势统计
- 用例自愈/定位器自动修复

具体先动哪块，取决于你想把它当**长期维护的测试平台**，还是**短期跑通验证**。
