# 第四档 P1：配置安全加固（待拍板，未实施）

> 只讲 P1。P2（权限分级）/ P3（操作审计）/ P4（部署运维）后续再议。
> 本文每一节都是「专业说法 → 大白话 → 具体怎么改 → 怎么验证 → 不做会怎样」五段式。
> 涉及当前代码的位置都标了文件名和行号，方便你对照。

---

## 0. 一页速览

| # | 项目 | 现状（web/yikeui/settings.py） | 改成 | 停机要求 |
|---|------|------------------------------|------|----------|
| P1-1 | SECRET_KEY | 第 22 行硬编码 `django-insecure-oo&+lr$...` | 外置到 `.env`，缺失时拒绝启动 | 换 key 需重启，**所有已登录用户被登出** |
| P1-2 | DEBUG | 第 24 行写死 `True` | 默认 `False`，开发用 `YIKEUI_DEBUG=1` 打开 | 重启 |
| P1-3 | 数据库凭据 | 第 82 行默认密码 `123456` 写在代码里，且用 `root` | 删默认值 + 建专用账号（最小权限） | 重启（建账号需 DBA 操作一次） |
| P1-4 | 传输/浏览器侧 | 未设置任何 `SECURE_*` | 按「局域网 http」和「公网 https」两档分别开 | 重启 |
| P1-5 | 日志与错误可见性 | 无 `LOGGING` 配置 | 文件日志 + `ADMINS`，关掉 DEBUG 后仍能查错 | 重启 |

前置依赖：P1-2 关掉 DEBUG 之后，**静态文件必须有人伺服**。现在 `web/static/` 里已有本地化的
`bootstrap.min.css`、`echarts.common.min.js`（早期报告依赖的 CDN 已经失效并本地化了），
所以只要把 `static/` 交给 whitenoise 或 nginx 即可，工作量很小。

---

## 1. 为什么这一档排第一：这个平台本身就是个「密码本」

**专业说法**：本平台属于典型的 **凭据聚合系统（credential aggregation system）** —— 用例步骤的
「操作值」列存放被测系统的登录账号与密码（如 `crm_login` 的 `input1`/`input2`），
通知配置里存 SMTP 授权码，数据库里存全部执行结果与报告。一旦 Web 层被接管，攻击者拿到的是
**下游多个被测系统的可用凭据**，影响面从"这个平台挂了"放大到"CRM / 智慧考试系统的账号泄露"。
按风险优先级排序，配置类加固（低成本、高收益）应当先于权限分级与审计。

**大白话**：这网站里存着你各个被测系统的账号密码。别人只要能登进来当管理员，
不用去猜 CRM 的密码，直接在你这儿翻就行。而现在的配置，等于把"万能钥匙"和"钥匙串"放在同一个抽屉里。

---

## P1-1 SECRET_KEY 外置与轮换

### 专业说法

`SECRET_KEY` 是 Django 的**根密钥（root secret）**，用于 HMAC 签名：
Session cookie（`django.contrib.sessions` 的 sessionid 签名）、密码重置 token、
`django.core.signing` 的通用签名、`messages` 框架的 cookie 签名、CSRF token 的一部分。

威胁模型：该值目前硬编码在 `settings.py`，会随代码目录一起被拷贝、打包、截图、发给同事。
**已知 SECRET_KEY = 可伪造任意用户的会话**（在 `db` 后端下 session 数据存在库里，
但 cookie 里的 sessionid 靠签名校验，签名密钥已知即可冒充 admin），即 **垂直越权 + 完全接管**。
若将来把 `SESSION_ENGINE` 改成 `signed_cookies` 且序列化器是 pickle，则可进一步导致
**反序列化远程代码执行（deserialization RCE）**——这是 Django 历史上真实出现过的漏洞类型。

### 大白话

SECRET_KEY 就是"给每个登录用户的手腕上盖的隐形印章"。别人拿到你的印章坯子，
就能给自己盖一个"我是 admin"的章，门卫（Django）验章是通过的，直接放行。
现在这个坯子就写在代码文件里，谁拷走你一份代码，谁就有了。

### 具体怎么改

1. 新建 `web/.env`（**不入库**）与 `web/.env.example`（入库模板）：

```ini
# .env.example —— 复制成 .env 后填真实值
YIKEUI_DEBUG=1
YIKEUI_SECRET_KEY=            # 留空则用 get_random_secret_key() 临时生成（仅开发）
YIKEUI_DB_NAME=yikeui
YIKEUI_DB_USER=yikeui
YIKEUI_DB_PASSWORD=
YIKEUI_DB_HOST=127.0.0.1
YIKEUI_DB_PORT=3306
```

2. `.gitignore` 追加 `.env`、`.env.*`（`.env.example` 例外）。

3. `settings.py` 顶部加载（推荐 `pip install python-dotenv`，一个纯 Python 小包，无编译依赖）：

```python
from django.core.exceptions import ImproperlyConfigured
from django.core.management.utils import get_random_secret_key
from dotenv import load_dotenv

load_dotenv(BASE_DIR / '.env')      # 文件不存在就忽略，不报错

def env_bool(name, default=False):
    v = os.environ.get(name)
    if v is None:
        return default
    return v.strip().lower() in ('1', 'true', 'yes', 'on', 'y')

DEBUG = env_bool('YIKEUI_DEBUG', False)     # 安全默认值：不配就是关

# SECRET_KEY：开发环境没配就现场随机生成一个（每次重启都变，够用且不会误上生产）；
# 生产环境没配就直接拒绝启动 —— 宁可起不来，也不能带着一个已知的弱密钥跑起来。
if DEBUG:
    SECRET_KEY = os.environ.get('YIKEUI_SECRET_KEY') or get_random_secret_key()
else:
    SECRET_KEY = os.environ.get('YIKEUI_SECRET_KEY')
    if not SECRET_KEY:
        raise ImproperlyConfigured('生产模式必须在 .env 里配置 YIKEUI_SECRET_KEY')
```

4. 生成新 key（一次即可，写入 `.env`）：

```
..\.python3\python.exe -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

5. **轮换（更换）SECRET_KEY 的影响，要提前知道**：
   - 所有已登录用户被强制登出（旧 sessionid 签名作废）—— 数据不丢，重新登录即可；
   - 已发出的"密码重置"链接失效；
   - `messages` 里未消费的 cookie 内容作废（无害）。
   所以挑一个没人跑执行的时段重启即可，**不需要迁移数据**。

### 怎么验证

- `.python3\python.exe manage.py check --deploy` 里不再出现 `SECRET_KEY` 相关警告；
- 故意在 `.env` 里删掉 `YIKEUI_SECRET_KEY` 并把 DEBUG 关掉，服务应当**启动失败**并给出明确提示（fail-fast 生效）；
- 重启后访问首页应跳登录页（证明会话已失效）。

### 不做会怎样

代码只要外流一份（拷给同事、发群里、备份到网盘），拿到的人无需破解即可直接构造管理员会话，
查看/导出全部用例与其中的被测系统账号密码，并可执行任意用例。属于"一次泄露，全线失守"。

---

## P1-2 DEBUG 默认关闭

### 专业说法

`DEBUG=True` 在生产环境有四类后果：

1. **信息泄露**：未捕获异常会返回完整 traceback 页面，包含源码片段、局部变量（Django 会对
   `PASSWORD`/`SECRET_KEY` 等做部分脱敏，但不完整）、SQL 语句、绝对路径与 URL 路由表。
2. **内存无上限增长**：Django 只有在 DEBUG 时才会把每条 SQL 存进 `connection.queries`。
   本平台是长驻进程（还带 30 秒轮询的调度线程 + 后台执行线程），跑一两天会攒下几十万条查询记录，
   表现为**内存持续上涨直到 OOM**。
3. **静态文件由 `runserver` 代管**：关掉 DEBUG 后 Django 不再伺服 `static/`，页面会掉样式。
4. **ALLOWED_HOSTS 被绕过**：DEBUG 时 Django 自动放行 `localhost / 127.0.0.1 / [::1]`，
   掩盖了 Host 头配置错误，等关掉才发现全部请求 400。

同时，关闭 DEBUG 是**安全默认值（secure by default）**原则的体现：想开必须显式声明。

### 大白话

DEBUG 开着的时候，网站一出错就把"家底"全打印在页面上：文件在哪儿、数据库怎么查的、
哪个变量是什么值，全给人看。而且它还偷偷记账，每执行一条 SQL 就记一笔，
你这是要 7×24 小时挂着跑定时任务的服务，账本迟早把内存撑爆。
另外它现在还帮你"兜着"域名配置，等哪天你正式关掉它，可能整个站直接打不开。

### 具体怎么改

1. `settings.py`：`DEBUG = env_bool('YIKEUI_DEBUG', False)`（见 P1-1 片段）；
   开发时在 `web/.env` 里写 `YIKEUI_DEBUG=1` 即可回到现在的效果。

2. **必须同步处理三件事**（否则关掉 DEBUG 会"网站变白"）：

   - **ALLOWED_HOSTS**：现在第 29-30 行是硬编码列表，够用；部署到新域名时用
     `YIKEUI_ALLOWED_HOSTS` 环境变量追加，避免改代码。
   - **静态文件**（二选一）：
     - **A. whitenoise（推荐先做，成本 5 分钟）**：`pip install whitenoise`，
       在 `MIDDLEWARE` 的 `SecurityMiddleware` 下面加一行 `whitenoise.middleware.WhiteNoiseMiddleware`，
       再跑一次 `manage.py collectstatic`。纯 Python、不需要 nginx，Windows 单机部署很合适。
     - **B. nginx 托管**：等 P4 做 waitress + nginx 时一起处理；
       在那之前先走 A，不要裸奔。
   - **错误可见性**：见 P1-5，必须配 `LOGGING`，否则 500 之后你只能看到一句"服务器错误"。

### 怎么验证

- `manage.py check --deploy` 中 `DEBUG` 警告消失；
- 访问一个不存在的 URL：应当看到简洁的 404 页面（而不是黄色调试页）；
- 看首页样式是否正常（证明静态文件有人伺服）；
- 观察任务管理器里进程内存：关掉 DEBUG 后不再随执行次数持续上涨。

### 不做会怎样

出错页面泄露路径/SQL/配置片段（信息泄露，属于合规红线级别的低级漏洞）；
长跑进程 OOM 崩掉 —— 而且崩的时候你可能正跑着定时任务，任务状态会卡在"执行中"；
另外一旦哪天有人帮你关了 DEBUG，静态资源 404，全站样式丢失，排查起来很懵。

---

## P1-3 数据库凭据外置 + 专用账号（最小权限）

### 专业说法

现状：`DATABASES['default']['PASSWORD']` 默认值 `'123456'` 硬编码（第 82 行），
且使用 MySQL 的 `root` 超级用户。这违反两条基本原则：

- **配置与代码分离（12-Factor App，Config）**：凭据属于环境，不属于代码；
- **最小权限原则（Principle of Least Privilege）**：应用账号只应拥有自身 schema 的
  DML/DDL 权限，不应具备 `SUPER`、`FILE`、跨库访问、账号管理等能力。
  一旦出现 SQL 注入或命令执行类漏洞，`root` 会把损失从"一个库"放大到"整个 MySQL 实例"。

### 大白话

现在相当于：应用拿着数据库"总管理员"的钥匙在干活，而且这把钥匙的密码还写在代码文件里。
正确的做法是给它配一把只能开自己家门的钥匙，密码写在只有这台机器才有的小本子（.env）上。

### 具体怎么改

1. `settings.py` 去掉所有默认值中的秘密部分：

```python
_DB_PASSWORD = os.environ.get('YIKEUI_DB_PASSWORD')
if not _DB_PASSWORD and not DEBUG:
    raise ImproperlyConfigured('生产模式必须配置 YIKEUI_DB_PASSWORD')
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': os.environ.get('YIKEUI_DB_NAME', 'yikeui'),
        'USER': os.environ.get('YIKEUI_DB_USER', 'yikeui'),
        'PASSWORD': _DB_PASSWORD or '',
        'HOST': os.environ.get('YIKEUI_DB_HOST', '127.0.0.1'),
        'PORT': os.environ.get('YIKEUI_DB_PORT', '3306'),
        'OPTIONS': {'charset': 'utf8mb4'},
    }
}
```

2. 建专用账号（一次性，DBA/你自己执行）：

```sql
CREATE USER 'yikeui'@'localhost' IDENTIFIED BY '<一个强密码>';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, DROP, INDEX,
      REFERENCES, CREATE TEMPORARY TABLES, LOCK TABLES
  ON `yikeui`.* TO 'yikeui'@'localhost';
FLUSH PRIVILEGES;
```

> **坑（很重要）**：跑 `manage.py test` 时 Django 要自己建/删 `test_yikeui` 库，
> 上面的权限不够。解决办法是**测试环境仍用 root**：在跑测试的终端里临时
> `set YIKEUI_DB_USER=root` + `set YIKEUI_DB_PASSWORD=...`，
> 或者给账号追加 `GRANT ALL ON test_yikeui.* TO 'yikeui'@'localhost';`（不推荐用在生产机上）。
> 我倾向前者：生产机根本不跑测试。

3. 把 `.env` 里的 `YIKEUI_DB_USER` / `YIKEUI_DB_PASSWORD` 换成新账号。

### 怎么验证

- 改完重启，随便点几页能正常读写（增删改一条用例再改回来）；
- 用专用账号连 MySQL 执行 `SHOW DATABASES;`，应当**只看到 yikeui（和 information_schema）**；
- 尝试 `CREATE DATABASE hack;`，应当被拒绝（证明权限收窄生效）。

### 回滚

把 `.env` 里的 `YIKEUI_DB_USER` 改回 `root`、密码改回原值，重启即可。零数据风险。

### 不做会怎样

代码外传 = 数据库凭据外传。而且应用拿 root 跑，将来万一有注入类漏洞，
攻击者能读 `/`、`DROP` 别的库、往磁盘写文件（`FILE` 权限），损失不可控。

---

## P1-4 传输与浏览器侧：分两档开，别一口气全开

### 专业说法

这一组是 **HTTP 安全响应头与 Cookie 属性**：

| 设置 | 作用 | 局域网 http（现在） | 公网 https（将来） |
|------|------|--------------------|--------------------|
| `SECURE_CONTENT_TYPE_NOSNIFF = True` | 加 `X-Content-Type-Options: nosniff`，阻止 MIME 嗅探 | ✅ 开 | ✅ 开 |
| `SECURE_REFERRER_POLICY = 'same-origin'` | 控制 Referer 泄露 | ✅ 开 | ✅ 开 |
| `X_FRAME_OPTIONS` | 点击劫持防护（`XFrameOptionsMiddleware` 已在 MIDDLEWARE 中） | 默认 DENY，报告页已用 `@xframe_options_sameorigin` 局部放行 | 同 |
| `SECURE_SSL_REDIRECT` | http 自动跳 https | ❌ 开了直接打不开 | ✅ 开 |
| `SECURE_HSTS_SECONDS` | HSTS，强制浏览器只走 https | ❌ 同上 | ✅ 开（建议 31536000） |
| `SESSION_COOKIE_SECURE` / `CSRF_COOKIE_SECURE` | Cookie 只在 https 传输 | ❌ 开了会导致**登录不上** | ✅ 开 |
| `SECURE_PROXY_SSL_HEADER` | 反代后面识别真实协议 | 不需要 | nginx 后面才需要 |
| `CSRF_TRUSTED_ORIGINS` | 允许的来源，**必须带 scheme** | 现在写的 `http://...` 是对的 | 换成 `https://...` |

### 大白话

这几个开关分两拨：一拨是"怎么开都安全"的，现在就能开；
另一拨是"只有上了 https 才能开"的，**现在开了等于把自己锁在门外**——
浏览器只在 https 下才肯带这些 Cookie，你用 http 访问就会一直登不上去、表单提交一直报 CSRF 错误。
所以这张表要照着场景抄，不能照单全抄。

### 具体怎么改

```python
# 与传输协议无关的，永远开
SECURE_CONTENT_TYPE_NOSNIFF = True
SECURE_REFERRER_POLICY = 'same-origin'

# 只有 https 才开的（用环境变量控制，部署时打开）
_SECURE = env_bool('YIKEUI_HTTPS', False)
SECURE_SSL_REDIRECT = _SECURE
SESSION_COOKIE_SECURE = _SECURE
CSRF_COOKIE_SECURE = _SECURE
SECURE_HSTS_SECONDS = 31536000 if _SECURE else 0
SECURE_HSTS_INCLUDE_SUBDOMAINS = _SECURE
SECURE_HSTS_PRELOAD = _SECURE
```

### 不做会怎样

影响相对温和（相比 P1-1/2/3），主要是：缺少 nosniff 时浏览器可能把用户上传/生成的内容
当脚本执行（本报告目录下的 HTML 正是这类"可被浏览器当页面打开的内容"）；
上 https 后不加 secure 标记，会话 Cookie 可能在明文 http 上被截获。

---

## P1-5 日志与错误可见性

### 专业说法

关掉 DEBUG 之后，未捕获异常不再回显页面，Django 的默认行为是：
把 500 的 traceback 通过 `mail_admins` 发给 `ADMINS`（需要 SMTP，否则静默丢弃），
其余日志走 `logging` 的默认配置（只输出到控制台）。
长驻进程必须配置 **持久化、可轮转的日志（RotatingFileHandler）**，否则：
排查问题全靠"刚才控制台一闪而过的那行红字"，而 Windows 的 cmd 窗口缓冲区一翻就没了。

### 大白话

关掉调试模式以后，网站出错就不再把原因写在页面上了 —— 这是对的，但不能变成"瞎子"。
得让它把错误老老实实写进一个文件，出事了去翻文件。

### 具体怎么改

`settings.py` 追加（放在 `REPORTS_DIR` 那一段附近）：

```python
LOG_DIR = BASE_DIR / 'logs'
LOG_DIR.mkdir(exist_ok=True)

LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'std': {'format': '%(asctime)s [%(levelname)s] %(name)s: %(message)s'},
    },
    'handlers': {
        'console': {'class': 'logging.StreamHandler', 'formatter': 'std'},
        'file': {
            'class': 'logging.handlers.RotatingFileHandler',
            'filename': str(LOG_DIR / 'yikeui.log'),
            'maxBytes': 10 * 1024 * 1024,   # 10MB
            'backupCount': 5,
            'formatter': 'std',
            'encoding': 'utf-8',
        },
    },
    'loggers': {
        'django': {'handlers': ['console', 'file'], 'level': 'INFO', 'propagate': True},
        # 请求层面的 4xx/5xx：django.request 的 WARNING/ERROR 是排障主力
        'django.request': {'handlers': ['file'], 'level': 'ERROR', 'propagate': False},
        # 登录失败、CSRF 失败等安全事件
        'django.security': {'handlers': ['file'], 'level': 'WARNING', 'propagate': False},
    },
    'root': {'handlers': ['console', 'file'], 'level': 'INFO'},
}

ADMINS = [('陆国成', 'luguocheng@moyi365.com')] if not DEBUG else []
SERVER_EMAIL = os.environ.get('YIKEUI_SERVER_EMAIL', 'noreply@yikeui.local')
```

> 注意：`ADMINS` 生效需要 SMTP。SMTP 可以直接复用"通知设置"里那套（另开一个专用发件账号），
> 也可以先只落文件、不做邮件告警（我建议 P1 阶段**只落文件**，邮件告警等 P4 有了常驻 SMTP 再说）。

另外 `web/logs/` 要加进 `.gitignore`，避免日志进代码目录。

### 怎么验证

- 重启后人为触发一次 500（比如临时把某个 URL 改坏），`web/logs/yikeui.log` 里应出现完整 traceback；
- 连续触发多次，确认超过 10MB 会切成 `yikeui.log.1` 而不是无限变大。

### 不做会怎样

线上出问题时没有现场，只能靠复现；而自动化平台的问题（浏览器起不来、元素超时）
往往跟当时环境强相关，很难复现，最后变成"重启一下就好了"这种治标不治本的处理。

---

## 汇总：不做会怎样

| 风险 | 触发条件 | 后果 | 严重度 |
|------|----------|------|--------|
| 会话伪造 / 平台被接管 | SECRET_KEY 随代码外流 | 攻击者以管理员身份登录，导出全部用例与被测系统账号密码 | **高** |
| 信息泄露 | 任意未捕获异常 + DEBUG=True | 页面回显源码、SQL、路径、配置片段 | **高** |
| 进程 OOM | DEBUG=True + 长驻运行 | 服务在无人值守时崩掉，定时任务状态卡死 | 中高 |
| 数据库失守 | 代码外流（含 root 密码）/ 注入漏洞 | 整个 MySQL 实例被读改删 | **高** |
| 样式丢失 / 站点 400 | 贸然关闭 DEBUG 而未配静态与 ALLOWED_HOSTS | 全站不可用 | 中（可立即回退） |
| 无法排障 | 关闭 DEBUG 但未配日志 | 线上问题无现场 | 中 |

---

## 实施顺序（建议一次做完，约 30 分钟 + 1 次重启）

1. `pip install python-dotenv`（如选 whitenoise 则一并装）；
2. 改 `settings.py`（DEBUG / SECRET_KEY / DB / SECURE_* / LOGGING 一次到位）；
3. 写 `web/.env` 与 `web/.env.example`，`.gitignore` 加 `.env`、`web/logs/`；
4. 生成新 SECRET_KEY 写进 `.env`；
5. MySQL 建 `yikeui` 专用账号（可选，可推迟到正式部署那台机器再做）；
6. `manage.py collectstatic`（若选 whitenoise）+ `manage.py check --deploy` + 跑一遍测试；
7. 挑个没人跑执行的时段重启服务，确认：能登录、页面有样式、404 是简洁页、日志在写。

**回滚**：`.env` 里把 `YIKEUI_DEBUG=1` 改回去、用户改回 `root` 即恢复现状；
`settings.py` 的改动本身不破坏现有行为（开发模式下与现在等价）。

---

## 需要你拍板的 4 个选择

1. **配置载体**：A. `python-dotenv` + `.env` 文件（推荐，改配置不用改代码、跨平台一致）
   vs B. 纯系统环境变量 + 启动脚本里 `set`（零依赖，但 Windows/Linux 两套脚本）。
2. **静态文件**：A. 现在装 whitenoise（5 分钟，立刻可以关 DEBUG）
   vs B. 等 P4 的 nginx 一起做（在那之前 DEBUG 只能继续开着，风险敞口保留）。
3. **DB 专用账号**：A. 现在就在本机 MySQL80 上建（顺手把权限收窄）
   vs B. 等部署到 Linux 时再做（本机继续用 root，可接受）。
4. **SECRET_KEY 轮换时机**：A. 现在就换（所有人登出一次，反正目前就你自己在用）
   vs B. 打包进"正式上线"那天一起换。

我的建议：**1A + 2A + 3A + 4A**，一次做完，代价最小。
